import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
const Feed = () => {
  return (
    <SafeAreaView>
      <Text>Feed</Text>
    </SafeAreaView>
  );
};
export default Feed;
